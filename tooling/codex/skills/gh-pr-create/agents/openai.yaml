interface:
  display_name: "GitHub PR Create"
  short_description: "Create GitHub PRs with repo rules and labels"
  default_prompt: "Use $gh-pr-create to prepare and create a GitHub pull request with template, issue links, milestone handling, appropriate existing repository labels, and a Korean noun-phrase title by default unless the user explicitly asks for another language or style."

policy:
  allow_implicit_invocation: false
